A script to generate user NFS directories based on the OU and username of a person.
You can specifcy the NFS server, OU's to check for and the quota type.
If the user already exists and has a home directory it will skip.
At the end if will display the quota and usage on each user.
